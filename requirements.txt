# ===========================================================
# 必需依赖（核心功能：GUI + 图像处理）
# ===========================================================
PySide6==6.9.1
numpy==1.26.4
opencv-python==4.11.0.86

# ===========================================================
# 可选依赖：内容识别填充 - pypatchmatch (C++ PatchMatch)
#   纯 Python 回退方案无需此包，但速度较慢
# ===========================================================
# pip install pypatchmatch
# PyPatchMatch==1.0.2

# ===========================================================
# 可选依赖：内容识别填充 - LaMa (深度学习，效果最好)
#   需要 torch + torchvision，见下方 GPU 依赖
# ===========================================================
# pip install simple-lama-inpainting
# simple-lama-inpainting==0.1.2
# Pillow==9.5.0            # simple-lama-inpainting 的依赖

# ===========================================================
# 可选依赖：GPU 色调协调 - libcom (PCTNet)
#   纯 CPU 回退方案 (Reinhard) 无需此包
#   需要 torch + torchvision，见下方 GPU 依赖
# ===========================================================
# pip install libcom einops
# libcom==0.1.4
# einops==0.8.2

# ===========================================================
# GPU 依赖 (LaMa 和 libcom 共用，二选一即需安装)
#   CUDA 13.0 版本，从 PyTorch 官方源安装:
#   pip install torch==2.10.0+cu130 torchvision==0.25.0+cu130 --index-url https://download.pytorch.org/whl/cu130
# ===========================================================
# torch==2.10.0+cu130
# torchvision==0.25.0+cu130
